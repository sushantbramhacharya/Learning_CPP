# Fundamentals of C++
## Namespaces
Namespaces are the names of library
We can use different namespaces type in c++
- std::cout
    - Direct use of namespaces
- using namespace std;
    - When i have to use namespaces in globally
- using std::cout
    - Using cout of std onlys

## io stream
I/O stream will store every input you inter and gives it to CIN

## Vector
Vector are dynamic datastructure where you can store data in it.Without worring about size of vector. 
- You can simple push and pop from a vector by using vector.push_back() or .pop_back()
- Accesing the size of vector can be done by .size()